import { FilterPhoneticsPipe } from './filter-phonetics.pipe';

describe('FilterPhoneticsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPhoneticsPipe();
    expect(pipe).toBeTruthy();
  });
});
